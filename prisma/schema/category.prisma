model Category {
    id            String  @id @default(cuid())
    title         String?
    slug          String? @unique
    description   String?
    image         String?
    startingPrice Float?

    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt

    services Service[]
}
